{
  "name": "guilded-game-overlay",
  "version": "1.0.0",
  "main": "index.js",
  "install": "node install.js",
  "scripts": {
    "preinstall": "cd ./electron-overlay && npm install && cd ../node-ovhook && npm install && cd .."
  },
  "repository": "https://github.com/TeamGuilded/game-overlay.git",
  "dependencies": {
  }
}
